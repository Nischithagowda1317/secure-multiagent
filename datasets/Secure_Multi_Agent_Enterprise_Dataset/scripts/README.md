# Scripts

- `validate_dataset.py`: standard-library validator for IDs, relationships, documents, RBAC coverage, and deterministic demo facts.
- `example_queries.sql`: sample PostgreSQL queries for Atlas status, workloads, sales analysis, RBAC, secure RAG, approvals, and agent traces.

Run validation from the package root:

```bash
python scripts/validate_dataset.py
```
